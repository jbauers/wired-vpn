version: '3'
services:
  oidc:
    build: .
    ports:
      - "80:8080"
    networks:
      - redis

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
    networks:
      - redis

networks:
  redis:
    driver: bridge
