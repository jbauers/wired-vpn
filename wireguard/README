Take care of this folder that could contain private keys.
Be sure that this folder never is published.

The script 'wireguard.sh' called by 'index.py' creates confiugration files for
Wireguard stored in this folder as:

 * wireguard.conf     Server configuration file, including private key
 * <USER_EMAIL>.conf  User configuration files, including private keys

This folder is preserved between container restarts as a Docker volume. See
'Dockerfile'.

